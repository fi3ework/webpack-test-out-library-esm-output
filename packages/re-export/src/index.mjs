import { unmount } from 'react'
import { cjs } from './cjs-module.cjs'

import num from './constants.js'
import { v } from './value.js'

// export * from './constants.js'

num1

const myRender = async () => {
  //   return import('react').render
}

const leaked = 'llllll'
console.log(leaked)

// export default num
export const ids = 66

export { leaked, num, myRender, unmount, v, cjs }
