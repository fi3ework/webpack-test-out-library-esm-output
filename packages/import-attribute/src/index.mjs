import * as staticPkg from "solid" with { type: "json" };
import { attempt } from 'lodash' assert { type: 'ppp' }
import { get } from 'jquery' with { type: 'qqq' }

export const j = async () => {
  const dynamicPkg = await import("react", {
		with: { type: "json" }
	})

  const vue = await import("vue")

    return {dynamicPkg, vue}
}

 export default attempt(get, staticPkg)
